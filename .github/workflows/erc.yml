name: ERC

on:
  push:
    branches: [main]

jobs:
  erc:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/kicad/kicad:9.0
      options: --user root

    steps:
      - uses: actions/checkout@v4

      - name: Run ERC
        run: kicad-cli sch erc *.kicad_sch